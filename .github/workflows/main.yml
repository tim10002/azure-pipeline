name: Shell Script

on: [push]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run script
      run: |
        echo "hello world ~"
        uname -a
      
      
